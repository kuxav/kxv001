name: Docker Image CI - Nahida x Kuxav

on:
  push:
    branches: [ "kuxav" ]
  pull_request:
    branches: [ "kuxav" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
